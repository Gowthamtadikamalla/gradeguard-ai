# Requirements for AWS Lambda Container
# Using numpy 1.x for Lambda compatibility (numpy 2.x requires GCC 9.3+)

# Web Framework
fastapi==0.115.0
mangum==0.18.0

# Data Validation
pydantic==2.9.0

# Machine Learning - Compatible with Lambda Python 3.10
# Using numpy 1.24.4 for compatibility with numba 0.57.1 (numba requires numpy < 1.25)
# Using scikit-learn 1.1.3 to match local environment
# Compatibility patches in src/sklearn_compat.py handle 1.0.2 model loading
scikit-learn==1.1.3
pandas==2.2.3
numpy==1.24.4
joblib==1.4.0

# Gradient Boosting Models (optional - only if model uses them)
xgboost==1.7.6
lightgbm==3.3.5

# AWS
boto3==1.35.0

# AI Agent
httpx<0.28
huggingface-hub>=1.1.5

# Explainability - SHAP with compatible versions
# Using compatible versions: numpy 1.24.4 + numba 0.57.1 + shap 0.42.0
# numba 0.57.1 requires numpy < 1.25, so we use numpy 1.24.4
# These versions work together and have pre-built wheels (no compilation needed)
shap==0.42.0
numba==0.57.1
llvmlite==0.40.0

