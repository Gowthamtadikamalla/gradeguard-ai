# Requirements for local benchmarking with Python 3.11
# Matches SageMaker training environment (scikit-learn 1.0.2, numpy 1.24.4)

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Data Validation
pydantic==2.9.0
pydantic-settings==2.5.0

# Machine Learning - Must match AWS Lambda and training environment (scikit-learn 1.0.2)
# IMPORTANT: Using scikit-learn 1.0.2 to match cloud deployment
# This ensures the locally trained model works in both local and AWS environments
scikit-learn==1.0.2
pandas==2.2.3
numpy==1.24.4
joblib==1.4.0

# Gradient Boosting Models
xgboost==1.7.6
lightgbm==3.3.5

# Utilities
python-dotenv==1.0.1
requests==2.32.3

# AI Agent (minimal for benchmarking)
httpx==0.27.0
huggingface-hub>=1.1.5

# SHAP (compatible versions)
shap==0.42.0
numba==0.57.1
llvmlite==0.40.0

